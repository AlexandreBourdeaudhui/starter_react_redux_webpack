/*
 * Types
 */


/*
 * initialState
 */
const initialState = {};


/*
 * Reducer
 */
export default (state = initialState, action = {}) => {
  switch (action.type) {
    /*
     * Default
     */
    default:
      return state;
  }
};


/*
 * Actions Creator
 */
